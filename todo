- aller check les nouvelles features genres les journaux et tout
- ce qui est dans la pipeline (diag system, actions/config panel)
- update liste de features ? "Yunohost c'est aussi ..."

# old todo

- app backup/restore -> migration, app fallback, cloning (effet streisand) -> avantage de la standardisation
- backup de schrodinger / gros CI
- friends monitors each others
